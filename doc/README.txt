For creating structured plain text documents. PlainText is a text editor for plain text data documents such as TaskPaper and Markdown. Unlike programmer text editors

Documents are divided into a tree of sections. Each section has a type and text content. Sections optionally have tags. Tags have a name and optional value.

- Manipulate documents by section.
- Filter documents with query language. (compile down to XPath)